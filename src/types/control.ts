export interface AppInfo {
  name: string;
  path: string;
}

export interface ProcessInfo {
  name: string;
  pid: number;
  processName: string;
}

export interface UserPaths {
  home: string;
  desktop: string;
  documents: string;
  downloads: string;
  pictures: string;
}